import 'package:flutter/material.dart';

class ScreenAdminViewTournament extends StatelessWidget {
  const ScreenAdminViewTournament({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: SafeArea(child: Text('Tour')),
      
    );
  }
}