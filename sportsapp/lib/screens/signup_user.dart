import 'package:flutter/material.dart';

class ScreenSignupUser extends StatelessWidget {
  const ScreenSignupUser({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      
    );
  }
}